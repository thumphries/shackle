<html>
  <head> 
    <link rel="stylesheet" href="../static/index.css"/>
  </head>
  <body>
    <h1>${title}</h1>
    <span>Published: ${date}</span>
    $(if [ -n "${nwords+set}" ]; then echo "<span>Words: ${nwords}</span>"; fi)
    <div>
      ${body}
    </div>
  </body>
</html>
